REGULI RTDB — AutoAlert v1.5.0
================================

1. Deschide:
   https://console.firebase.google.com/project/autoalert-89b80/database/autoalert-89b80-default-rtdb/rules

2. Șterge tot. Lipește întreg fișierul database.rules.json din folderul AutoAlert.

3. Apasă PUBLISH.

SAU: push pe master cu database.rules.json — GitHub Action deployează automat dacă secretul FIREBASE_TOKEN e setat.

IMPORTANT v1.5.0:
- families NU mai are ".read": "auth != null" (securitate)
- join cu cod folosește nodul inviteCodes/{COD}
- La prima deschidere după update, OWNER-ul sincronizează automat indexul inviteCodes

După publish: Ctrl+Shift+R în app.